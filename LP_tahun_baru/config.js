/**
 * Konfigurasi API untuk folder LP ini.
 * Ubah token di file ini saja — billinghub.js & billinghub.legacy.js memakai nilai ini.
 */
(function (w) {
  w.BILLINGHUB_CONFIG = {
    baseUrl: "https://billinghub.id/api",
    token: "XXXXXXXXXXX",
  };
})(window);
